---
import { Image } from "astro:assets";
import heroImage from "~/assets/moon.jpg";

const widths = [450, 800, 1200];
const sizes = "100vw";
---

<Image
  class="w-full object-cover"
  src={heroImage}
  widths={widths}
  sizes={sizes}
  alt="The ridged surface of the moon"
/>
